<% if @photo.title.size <= 12 %>
	<%= @photo.title %>
<% else %>
	<%= "#{@photo.title[0..11]}..." %>
<% end %>
<%= link_to (image_tag "btn_entry.gif",:border => 0), {:action => :edit_title, :photo => @photo.id}, {:remote => true, "data-type" => "html", "data-update" => "#{@photo.id}_title"} %>